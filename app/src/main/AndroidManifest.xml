<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.village">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Village">
        <activity
            android:name=".productwritng.ProductWriting"
            android:windowSoftInputMode="adjustPan"></activity>
        <activity android:name=".home.searchresult.SearchResult" />
        <activity android:name=".home.search.SearchActivity" />
        <activity android:name=".splash.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup.Name" />
        <activity android:name=".signup.Password" />
        <activity android:name=".signup.Id" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".home.Home"
            tools:ignore="Instantiatable" />
    </application>

</manifest>